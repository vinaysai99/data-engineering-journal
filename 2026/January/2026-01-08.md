# Daily Log: 2026-01-08

**Focus:** Python Refresher & ETL Basics  
**Status:** Completed ✅  
**Tools:** Python, Pandas, CSV, JSON

## 📝 Project: Motel Log ETL Generator
I built a mini-ETL pipeline to simulate processing guest logs from a motel. The goal was to re-establish muscle memory with Python scripting and file handling without relying on AI for syntax.

### 1. Data Generation (`generate_data.py`)
* **Goal:** Create a synthetic dataset simulating raw motel logs.
* **Key Logic:** Used `random` and `datetime` to generate realistic check-in dates and bill amounts.
* **Output:** Generated 400 rows of raw data in CSV format.

### 2. Transformation & Loading (`process_data.py`)
* **Goal:** Clean the data and prepare it for analysis.
* **Steps Taken:**
    * Ingested CSV data using `pandas`.
    * **Validation:** Filtered out erroneous records (Bills < $50).
    * **Transformation:** Calculated `Tax` (10%) and `Total Charge` columns.
    * **Loading:** Exported the cleaned dataset to JSON format for downstream usage.

### 🧠 Key Learnings
* Refreshed `csv.writer` syntax for custom data generation.
* Used `pandas` vectorization to calculate taxes efficiently (avoided slow loops).
* Validated data distribution using `df.describe()` before export.

---
*Next Steps: Explore AWS S3 for storing these files.*
